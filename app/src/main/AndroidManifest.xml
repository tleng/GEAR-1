<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mattmellor.gear" >

    <uses-permission android:name="android.permission.INTERNET" />
0
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_gear"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ReadArticleActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StoriesSelectionActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".DisplayVocabularyActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".userSettings"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".popUpRateArticle"
            android:theme="@style/AppTheme.CustomTheme" >
        </activity>
        <activity
            android:name=".SuggestedStoriesActivity"
            android:label="@string/title_activity_suggested_stories"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".LoginPopupActivity"
            android:theme="@style/AppTheme.CustomTheme" >
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
